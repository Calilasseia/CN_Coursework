Copy the files from this directory to a new project directory, as the starting HTML and CSS files for the new project, then rename the copies accordingly and edit them to suit the new project design. Once that is done, copy the required JavaScript library files to the project directory, and edit the HTML file to include them. After that, create new project files as needed, and update the HTML and CSS accordingly.